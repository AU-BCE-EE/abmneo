# Creates demo inputs

storage_demo_dig_01 <- list(
  type = 'series',
  dat = data.frame(time = , slurry_mass = ),
  storage_depth = 4,     
  area = 500,              
  temp_C = 15,
  resid_enrich = 0.9 
)

